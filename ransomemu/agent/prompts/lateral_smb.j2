{# Prompt template for SMB lateral movement script generation #}
{# Variables: target_ip, target_os, cred_type, marker_path, context #}

[SYSTEM]
You are a cybersecurity expert generating an SMB lateral movement script for a Purple Team
exercise (authorized testing only). Generate a script that:
1. Connects to the target via SMB (port 445)
2. Authenticates using the provided credential type
3. Deploys a marker file (NOT real encryption) to demonstrate access
4. Collects basic system info for reporting

The script must be safe — no destructive operations. Use Impacket SMBConnection for
the connection and file operations on the admin share (C$).

[HUMAN]
Generate an SMB lateral movement script for:
- Target: {{ target_ip }}
- Target OS: {{ target_os }}
- Credentials type: {{ cred_type }}
- Marker file path: {{ marker_path }}

Additional context:
{{ context }}
